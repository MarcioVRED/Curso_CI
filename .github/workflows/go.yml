name: Go

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ Aula_1 ]

jobs:

  test:
    runs-on: ${{ matrix.os_version }}
    strategy:
      matrix:
        go_version: ['1.18', '1.17', '>=1.18']
        os_version: ['ubuntu_latest', 'ubuntu-18.04', 'ubuntu-22.02', 'windows 11']
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: ${{ matrix.go_version }}
      
    - name: Build-DB
      run: docker-compose build
      
    - name: Create-DB
      run: docker-compose up -d

    - name: Test
      run: go test -v main_test.go 
      
  
  build:
    needs: test
    runs-on: ${{ matrix.os_version }}
    strategy:
      matrix:
        os_version: ['ubuntu_latest', 'ubuntu-18.04', 'ubuntu-22.02', 'windows 11']
    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: go build -v main.go
